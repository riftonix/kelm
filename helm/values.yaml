image:
  registry: ghcr.io
  repository: riftonix/kelm
  pullPolicy: IfNotPresent
  tag: "0.1.2"

serviceAccount:
  create: true
  annotations: {}
  name: ""

zarf:
  enabled: false

microservice:
  envs:
    IGNORED_NAMESPACES: >
      default,
      kube-system,
      kube-node-lease,
      kube-public,
      cert-manager

resources: {}
